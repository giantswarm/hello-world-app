name: Publish TechDocs
on:
  push:
    paths:
      - 'README.md'
      - 'docs/**' # markdown or any images in docs
      - 'helm/**/README.md'
      - '.github/workflows/**' # TODO: delete
    branches:
      - main
      - add-techdocs-workflow
  workflow_dispatch: {}

jobs:
  publish:
    uses: giantswarm/github/.github/workflows/publish-techdocs.yaml@add-techdocs-workflow
